#include <stdio.h>
#include <stdio.h>


// Conversão de comprimento em polegadas para centímetros

int main(){
    
    int P = 22 ; // declarando variável "P", recebe valor de 22
    float C ; // declarando variável "C", 

    //imprimindo valor de "P"
    printf(" exatos %d polegadas \n", P);

    C = P*2.54 ; // Conversão de "P" comprimento em polegadas para  "C" centímetros

    //imprimindo valor que "C" recebe
    printf("conversao para centimetros : %f \n",C) ;

    return 0;

}